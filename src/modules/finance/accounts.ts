/**
 * Идентификаторы счетов из сид-миграций.
 *
 * Отдельным модулем, а не в `yookassa/sync.ts`, где константа жила раньше:
 * теперь её читает и разбор выписки, а тянуть ради одной строки весь синк
 * (клиент HTTP, запись операций, правила категорий) в граф импорта незачем.
 */

/** Виртуальный счёт эквайринга — миграция `00000000000007_yookassa_account`. */
export const YOOKASSA_ACCOUNT_ID = "acc_yookassa";

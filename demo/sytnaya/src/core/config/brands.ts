import type { Brand, BrandId } from "../types";

/**
 * Реестр брендов группы.
 *
 * ЗДЕСЬ ТОЛЬКО ПРОВЕРЕННЫЕ ДАННЫЕ. Шести новых вывесок пока нет намеренно:
 * сайт sytnaya-dostavka.ru, на котором они перечислены, из окружения сборки
 * не открывается (политика egress режет весь исходящий HTTP), а придумывать
 * названия, кухни и цены нельзя — на этом демо разваливается в ту же секунду,
 * когда владелец его открывает.
 *
 * Как добавить бренд, когда данные появятся:
 *   1. Добавить запись сюда с `verified: true`.
 *   2. Вписать его id в `brands` тех кухонь, которые его готовят
 *      (`config/kitchens.ts`) — связь many-to-many живёт там.
 *   3. Проставить `homeKitchenId` — кухню, куда заказы бренда идут СЕЙЧАС.
 *      Без неё диспетчер не сможет показать выигрыш против нынешнего порядка.
 *
 * Больше ничего менять не нужно: витрина и диспетчер читают этот реестр.
 */
export const BRANDS: readonly Brand[] = [
  {
    id: "yaposha",
    name: "Сытый Япоша",
    cuisine: "Японская кухня, пицца",
    domain: "yaposha56.ru",
    homeKitchenId: "teatralnaya",
    verified: true,
    theme: {
      accent: "#E23A2E",
      accentInk: "#FFFFFF",
      surface: "#FFF7F2",
    },
  },
];

export const BRAND_BY_ID: ReadonlyMap<BrandId, Brand> = new Map(
  BRANDS.map((brand) => [brand.id, brand]),
);

/**
 * Сколько вывесок ещё предстоит завести.
 *
 * Держим числом, а не в голове: витрина показывает это в отладочной панели,
 * чтобы незаполненность нельзя было проглядеть перед показом владельцу.
 */
export const BRANDS_PENDING = 6;
